package flow.benchmark.utils;

public class GlobalSetup {
  // Reset database
  // Submit job to flink API
  // Get job id from /jars
  // Modify settings.json
  // Restart API server
  // Create a connector with db one
  // Create a connector with db two
}
